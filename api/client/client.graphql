input CreateClientRequest {
    name: String!
    email: String!
    password: String!
}

type Client {
    _id: String
    name: String
    email: String
    credit: Float
}

type Mutation {
    createClient (input: CreateClientRequest): Client
}